import React from 'react';

const Alert = () => {
	return (
		<div className="alert-container">
			<h1 className="alert">We couldn't find anything please try again</h1>
		</div>
	);
};

export default Alert;
